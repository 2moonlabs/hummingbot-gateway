{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "preferredNodeId": {
      "type": "string",
      "default": "",
      "description": "Optional Chainstack node ID (e.g., ND-123-456-789). If empty, the first running node matching the active chain/network is used."
    }
  },
  "additionalProperties": false
}
